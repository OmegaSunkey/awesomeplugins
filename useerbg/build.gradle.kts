version = "1.0.1"
description = "UserBG"

aliucord {
    changelog.set(
        """
	added usrbg.json
        * most of this plugin was made by halalking, i just fixed some things.
    """.trimIndent()
    )
}
