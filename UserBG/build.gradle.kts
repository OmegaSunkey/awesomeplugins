version = "1.0.4"
description = "UserBG"

aliucord {
    changelog.set(
        """
	added usrbg.json
	final fix 
        * most of this plugin was made by halalking, i just fixed some things.
	# 1.0.4
	set text hint 
    """.trimIndent()
    )
    author("HalalKing", 0)
}
