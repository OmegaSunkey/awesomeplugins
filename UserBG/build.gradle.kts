version = "1.0.2"
description = "UserBG"

aliucord {
    changelog.set(
        """
	added usrbg.json
	final fix 
        * most of this plugin was made by halalking, i just fixed some things.
    """.trimIndent()
    )
}
